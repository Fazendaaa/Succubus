image: golang

base:
  run: |
    echo "here"
    exit 0
  test:
    go test --verbose
  add:
    rules:
      go get $ARGV[0]
    env:
      ENV=${ENV}

dev:
  anal:
    gosec -fmt=sonarqujbe -out report.json ./..
  linter:
    golangci-lint run
  doc:
    go doc .
