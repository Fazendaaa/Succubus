image: golang

base:
  run: exit 0
  test:
    go test --verbose
  add:
    env:
      - ENV=${ENV}
    rules: go get $ARGV[0]

dev:
  anal:
    gosec -fmt=sonarqujbe -out report.json ./..
  linter:
    golangci-lint run
  doc:
    go doc .

extended:
  coverage: go test -cover ./...
