image: golang

base:
  run:

  test:
    go test --verbose
  add:
    env:
      - ENV=${ENV}
    command: go get $ARGV[0]

dev:
  anal:
    gosec -fmt=sonarqujbe -out report.json ./..
  linter:
    golangci-lint run
  doc:
    go doc .

extended:
  coverage: go test -cover ./...
