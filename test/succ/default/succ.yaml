image: golang

base:
  run:

  test:
    go test --verbose
  add:
    command: go get $PKG
    env:
      - ENV=${ENV}

dev:
  anal:
    gosec -fmt=sonarqujbe -out report.json ./..
  linter:
    golangci-lint run
  doc:
    go doc .
